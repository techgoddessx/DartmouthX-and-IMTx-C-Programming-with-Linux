#include <stdio.h>
// Store strings in arrays using pointers
int main(void){
	char array[] = {'a','b','c'};
	char * pointer;
	pointer = &array[0];
	for(int i = 0; i < 3; i++){
		printf("%d",pointer[i]);
	}
}
